/**
 * An ENUM for the state of the tests
 *
 * @type {{SUCCESS: string, WAITING: string, PENDING: string, FAILURE: string}}
 */
const State = {
    WAITING: 'WAITING',
    PENDING: 'PENDING',
    SUCCESS: 'SUCCESS',
    FAILURE: 'FAILURE',
}

module.exports = State;